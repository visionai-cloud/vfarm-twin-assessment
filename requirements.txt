fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27
# AI narration (optional at runtime — falls back to a template if unset)
openai>=1.0
# dev / test
pytest>=8.0
